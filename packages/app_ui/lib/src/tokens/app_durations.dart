/// Animation duration scale used across the app, outside the catalog
/// samples.
abstract final class AppDurations {
  /// 250 ms.
  static const fast = Duration(milliseconds: 250);

  /// 300 ms.
  static const base = Duration(milliseconds: 300);
}
