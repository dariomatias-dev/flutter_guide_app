import 'package:flutter/material.dart';

/// The app's color palette.
abstract final class FlutterGuideColors {
  /// Pure black.
  static const black = Color(0xFF000000);

  /// Primary dark background neutral.
  static const darkNeutral = Color(0xFF101214);

  /// Slightly lighter dark neutral (elevation 50).
  static const darkNeutral50 = Color(0xFF161A1D);

  /// Dark neutral for surfaces (elevation 200).
  static const darkNeutral200 = Color(0xFF22272B);

  /// Pure white.
  static const white = Color(0xFFFFFFFF);

  /// Light blue tint (shade 100).
  static const blue100 = Color(0xFFE9F2FF);

  /// Light blue-grey (shade 200).
  static const ligthBlue200 = Color(0xFFB3B9C4);
}
